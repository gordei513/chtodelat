import math
k = 12

if k > 1:
    for i in range(2, math.ceil(math.sqrt(k))):
         if (k % i) == 0:
            print(k +  "это составное число")
            break
    else:
        print(k - "это простое число")

else:
    print("ошибка")
